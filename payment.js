'use strict';
/**
 * תשלום — הכלל הברזל:
 *   ❌ אף פעם לא אוספים ולא שומרים מספר כרטיס גולמי אצלנו.
 *   ❌ אף פעם לא מזינים את כרטיס הלקוח לעליבאבא.
 *   ✅ הלקוח מקליד את הכרטיס בסליקה המאובטחת של ימות המשיח.
 *   ✅ אנחנו מקבלים רק אישור/אסמכתא — לא את הכרטיס.
 *   ✅ את עליבאבא אתה מזמין בכרטיס/חשבון העסק שלך — נפרד לגמרי.
 *
 * הפעלת סליקה בימות (בקצרה):
 *   1. פאנל ימות: הגדרות > סליקת אשראי — מחברים ספק סליקה ישראלי (טרנזילה וכו').
 *   2. יוצרים שלוחת "גביית תשלום" (למשל שלוחה שמספרה מוגדר ב-YEMOT_PAY_EXTENSION).
 *   3. הקו מעביר את המתקשר לשלוחה; הזנת הכרטיס קורית שם, בסביבה המאובטחת של ימות.
 *
 * שתי דרכים לחבר את זה לקוד:
 *   (א) פשוטה (מיושמת כאן): יוצרים את ההזמנה כ-pending, ומעבירים את המתקשר
 *       לשלוחת התשלום. סימון "שולם" נעשה ידנית בלוח הבקרה, או אוטומטית אם
 *       תגדיר בשלוחת התשלום קריאת API חזרה ל-/yemot/paid?order=ID אחרי הצלחה.
 *   (ב) מתקדמת: לבצע את הסליקה בתוך אותו handler באמצעות call.send(...) +
 *       call.blockRunningUntilNextRequest() ולקרוא את התוצאה. ראה README.
 */

const PAY_EXTENSION = process.env.YEMOT_PAY_EXTENSION || '/pay';

/**
 * מעביר את המתקשר לשלוחת התשלום המאובטחת של ימות.
 * זו הפעולה האחרונה ב-handler (השיחה עוברת שליטה לשלוחה).
 */
function handoffToPayment(call /*, amount */) {
  call.go_to_folder(PAY_EXTENSION);
}

module.exports = { handoffToPayment, PAY_EXTENSION };
